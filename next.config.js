module.exports = {
  // i18n: {
  //   locales: ["en"],
  //   defaultLocale: "en",
  // },
  output: "export",
  basePath: "/tongxing-waldorf-website",
  // target: "serverless",
  // webpack: (config, { isServer }) => {
  //   if (!isServer) {
  //     config.resolve.fallback.fs = false;
  //   }
  //   return config;
  // },
  images: {
    unoptimized: true,
  },
};
